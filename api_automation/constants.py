BASE_CREDENTIAL_PATH = "secrets"

# Google
GOOGLE_BASE_URL = "https://www.googleapis.com/auth"
GOOGLE_FOLDER = "google"

# Credentials are shared between APIs
GOOGLE_CREDENTIALS_FOLDER = "credentials"
GOOGLE_CREDENTIALS_FILE = "credentials.json"

# Tokens are unique to each API
GOOGLE_TOKENS_FOLDER = "tokens"
GOOGLE_TOKEN_FILE = "token.json"
